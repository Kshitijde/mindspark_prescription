<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doctor Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.2/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <center>
        <h1 style="margin-top: 3%;">My patients</h1>
    </center>
    <% for(var i = patients.length-1; i >= 0 ; i-- ) { %>
    <div class="card" style="width: 50%;margin-left: 25%; margin-top: 5%; padding: 2%;">
        <div class="card-body">
            <h4 class="card-title"><b>Patient: </b><%= patients[i].name %></h4>
            <button onclick="location.href='./patients/<%= patients[i]._id %>/history'">
                History
            </button>
            <button onclick="location.href='./patients/<%= patients[i]._id %>/prescribe'">
                Prescribe
            </button>
        </div>
    </div>
    <%}%>
</body>

</html>